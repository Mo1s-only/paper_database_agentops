# LLM 智能体可观测性、故障归因与多智能体系统优化 —— 文献调研（表格版）

> **调研范围**：8 篇论文（精读笔记见同目录 `<论文名>_论文精读笔记.md`）
> **调研主题**：Agent observability / failure attribution / MAS optimization
> **整理日期**：2026-09-15
> **说明**：本表按「论文 / 应用场景 / 实验设置 / 实验平台 / 数据集来源 / 结果验证 / 启示与局限性」七维逐篇列成表格，所有数字均可在对应精读笔记中溯源。

---

## 〇、总览表（一表看全 8 篇）

| # | 论文 | 单位 / 发表 | 核心问题 | 所在层次 | 一句话贡献 | 最强指标 |
|---|---|---|---|---|---|---|
| 1 | **AgentOps** (Enabling Observability of LLM Agents) | Data61, CSIRO（澳）；arXiv:2411.05285v2 | 该追踪什么？ | **元层（跨所有层）** | 系统映射研究，产出 9 类 Span 分类法 | Tracing/Monitoring **100%** vs Guardrails **35%** |
| 2 | **AgentSight** | Ubuntu 22.04 + Claude Code | 系统里发生了什么？ | **系统边界层** | eBPF 边界追踪，同时抓意图流与动作流 | 开销 **2.9%**；事件 **521→37** |
| 3 | **Watson** | Huawei Canada + Queen's Univ. | Agent 在想什么？ | **认知层** | 旁路替代 Agent 重建推理 | 零干扰（p>0.05）；推理发散 **30.9%** |
| 4 | **Who&When** | ICML 2025 | 谁在何时错？ | **归因层** | 提出并形式化自动故障归因 | Agent **53.5%** / Step **14.2%** |
| 5 | **AgentMonitor** | 港科大 + 清华 | 能提前预测性能吗？ | **预测层** | 一行包装 + 双组指标 + XGBoost | In-Domain Spearman **0.89** |
| 6 | **AgentDropout** | vLLM + A800 + Deepseek API | 怎么优化拓扑？ | **拓扑层** | 节点丢弃 + 边丢弃 | token **−21.4%~24.4%**，性能 **+0.47~2.19** |
| 7 | **Beyond Natural Language** | 清华 + 腾讯 + 北邮 | 用什么格式说？ | **表达层** | AutoForm 让 LLM 自主选格式 | token **−72.7%**；准确率 **+3.3~5.7%** |
| 8 | **DistServe** | OSDI 2024；北大 + 阶跃星辰 + UCSD | 资源怎么调度？ | **推理系统层（最底）** | prefill/decode 物理分离 | **7.4× 速率** / **12.6× 更严 SLO** |

---

## 一、论文总结（七维之一）

| 论文 | 核心问题 / 动机 | 核心方法 | 主要贡献 | 关键结论 |
|---|---|---|---|---|
| **AgentOps** | LLM 智能体的**自主性、非确定性、持续演化**带来 AI 安全担忧；从 DevOps 视角，可观测性是保障 AI 安全的前提，但"该追踪什么"缺乏统一认识 | **系统映射研究**（遵循 Kitchenham + Garousi 灰色文献指南）：GitHub 4 组检索词 + Google Search + 灰色文献 → 17 个工具 | ① 智能体工件**实体关系模型**（Fig.2）<br>② **AgentOps 分类法**（Fig.3）：**9 类 Span** + 7 项通用元数据 + 每类专属元数据 | **本文不建系统，它给领域"命名"** ——把"该追踪什么、到什么粒度"系统化为可共享的模板 |
| **AgentSight** | 智能体的"高层意图"与"底层系统动作"之间存在**语义鸿沟**；应用层插桩脆弱易绕过、系统层监控缺乏语义 | **边界追踪（boundary tracing）**：在应用代码**之外**的稳定边界（内核 + 网络）监控。三大组件：eBPF 统一探测 / 多信号因果关联引擎 / LLM 驱动语义分析 | ① 用**稳定的系统边界**绕开易变的框架实现<br>② 三信号关联（进程谱系 + 时间邻近 + 参数匹配）<br>③ "AI to watch AI" | **观测点的选择比观测手段更重要**；「插桩可以被绕过，边界不能被绕过」 |
| **Watson** | Agentware 采用「快速思考」模式，**推理不可见**；让模型输出 CoT 会**改变行为（准确率最多降 36%）**，事后追问的解释又**不忠实**——"既不能边想边说，也不能事后问" | **认知可观测性**：**旁路替代 agent** 镜像主 agent 的模型与解码参数；用 **FIM** 或 **RepCoT** 重建推理；**PromptExp 归因 + LLM-as-a-judge(n=10)** 验证；汇总成元推理总结 | ① 提出 AgentOps 第三层（认知层）<br>② 旁路重建 + 镜像原则<br>③ **三层可观测性模型**（操作/语义/认知） | **观测不能改变被观测对象**（RQ2 证明零干扰）；推理与答案强耦合但非绑定 |
| **Who&When** | MAS 的**故障归因**长期靠人工，昂贵且依赖专家；已有工作只造更细 benchmark，**未解决"评测结果 → 待改进组件"的映射** | 形式化**自动故障归因**新问题；用**干预式反事实定义「决定性错误」**（修好这一处就成功）；构建 Who&When 基准；设计 3 种 LLM 归因方法 + Hybrid | ① 提出并形式化一个全新研究问题<br>② **Who&When 基准**（127 系统 / 184 任务，84 人时三轮标注）<br>③ 诚实的负面结果报告 | **该问题远未解决**——Agent 级 53.5% / Step 级 14.2%，部分方法甚至低于随机 |
| **AgentMonitor** | MAS 配置难、性能只能事后观察；恶意 Agent 破坏系统安全 | **即插即用非侵入**：一行注册包装 → 采集**两类指标**（LLM 判官分数 + 图属性）→ **XGBoost 回归**预测下游表现；推理链插入 **post-edit** 对齐 LLM | ① 非侵入的性能预测框架<br>② 1,796 点数据集<br>③ 监控能力**顺手转化为安全能力** | **不解释机制，只预测结果**——工程上极其务实；廉价特征 + 少量昂贵采样即可预测 |
| **AgentDropout** | MAS **token 消耗巨大且效率低下**；已有方法（GPTSwarm/AgentPrune）**只剪边、不剪节点**，且**所有轮次用同一套策略** | 把通信图转为**可训练加权图**，用**无偏策略梯度**优化邻接矩阵：**① 节点丢弃**（剔除加权入出度最小者）→ **② 边丢弃**（核范数代替秩做低秩稀疏约束）；推理时 **DAGSample** 采样 | ① 「**节点冗余 vs 边冗余**」的正交分解<br>② 逐轮动态带来**双重收益**（性能 + completion token）<br>③ **40 条样本学拓扑** | **冗余有两种来源**——补上了 AgentPrune 忽视的"节点冗余"；**随机剪枝对照**排除了"链变短"的替代解释 |
| **Beyond NL** | 正面挑战一个普遍默认的假设——**自然语言（NL）是 LLM 推理和多智能体通信的最优格式** | **AutoForm**：在 CoT 提示中追加一句"考虑使用更结构化、更简洁的格式"，让 LLM **自主决定**格式（code/JSON/Markdown 表格/逻辑表达式/数学方程/列表）；两步版从 5 个输入推任务级通用格式 | ① 质疑共识 + 控制变量的范式文章<br>② **AutoForm**<br>③ **自发涌现的 ACL ≈ KQML** | **"格式"是被严重低估的优化维度**；一行 prompt 最高省 72.7% |
| **DistServe** | 现有系统把 **prefill** 和 **decode** 共置同批 GPU 混批以最大化吞吐量；**本文指出在 SLO 约束下这是代价高昂的错误选择**——两阶段干扰、调度失效、资源与并行度耦合 | 把两阶段**物理分离到不同 GPU**（disaggregation），各自独立优化；带宽感知的**放置算法**；**M/D/1 排队论**推导切换条件 | ① **重新定义优化目标**（per-GPU goodput）<br>② DistServe 系统 + 放置算法<br>③ 在线调度（抗气泡 + pull-based + replanning） | **目标函数选错，整个设计方向就错了**——比"分离"这个具体手段更有普适价值 |

---

## 二、应用场景（七维之二）

| 论文 | 主场景 | 衍生场景 | 适用边界（何时不该用） |
|---|---|---|---|
| **AgentOps** | **智能体全生命周期的可观测性基础设施**：原型→生产过渡、故障排查与根因定位、性能优化、评估与回归测试、提示词治理、人类反馈闭环、安全审计与合规、成本管理 | **三类隐含利益相关者**：智能体所有者 / 基础模型提供方 / 外部工具与智能体提供方（**Shared Accountability 责任共担**） | 分类法是**参考模板**而非强制标准；仅针对 LLM agents（含 agentic systems）；**不含**存储/传输/查询技术选型 |
| **AgentSight** | **安全检测**（首要）：间接提示注入、数据外泄识别、软件供应链攻击、上下文感知威胁判定 | **成本与可靠性**：推理循环/死循环检测、异常资源消耗告警、非确定性行为调试<br>**多智能体运维**：协调瓶颈定位、角色划分优化<br>**泛化**：生产 AgentOps 底座、审计取证、DevSecOps 集成 | 依赖 Linux + eBPF + OpenSSL；需内核 BPF 权限；**只能看不能拦**（无阻断能力） |
| **Watson** | **Agentware 的调试与故障诊断**（论文主场景）：读到推理偏差 → 一句 hint 即修正 | **按需的推理重建服务**（可观测性平台/IDE/CI-CD/生产看板，**零侵入零延迟**）；AI 安全审计；提示工程量化优化；过程监督数据生产 | 只覆盖**文本推理 agent**；成本不透明；重建忠实性缺人类金标准 |
| **Who&When** | **多智能体系统开发与调试**（论文主场景） | **统计视角**的低成本系统性改进（数据集级诊断而非逐条精确定位）；与系统级可观测性互补的根因分析 | ⚠️ **绝对性能远不可用**（Agent 53.5% / Step 14.2%）；长日志（>93 步）准确率趋近 0%；未验证"改后是否变好" |
| **AgentMonitor** | **MAS 架构选型 / A-B 测试前的预筛**——在跑昂贵端到端评测前先预测得分，只把高分配置送入真实评测 | **运行时成本控制**（偷看模式：10% 样本）；**Agent 角色/拓扑调优**（图指标提供可解释的优化方向）；**多智能体安全护栏**（post-edit）；**系统健康监控/异常识别** | **单 Agent 场景**（指标全为多 Agent 拓扑设计）；**冷启动场景**（XGBoost 需域内历史数据）；**架构变化剧烈**（跨架构仅 0.58）；**安全要求极高**（post-edit 只能缓解不能保证） |
| **AgentDropout** | **生产环境 MAS 的 token 成本优化**（论文主场景）：MAS token 消耗可达单智能体的 **30–70 倍** | **少样本新任务快速适配**（同类别拓扑可迁移、**训练样本越难迁移越好**）；**弱模型稳定性补强**；**成本敏感的边缘/端侧 MAS**；**提示工程与角色设计**（节点重要性度量可反向指导人工角色设计）；**多机/多传感器协同决策**（直接迁移） | DAG 约束**不支持循环辩论**；需一次离线拓扑学习；提升幅度随模型变强衰减 |
| **Beyond NL** | **多智能体系统降本**（token 直降 20%–74%，近乎零成本的降本手段）；**单模型推理提效**（+3.3~5.7%，纯 prompt engineering） | **符号/因果推理任务救急**（Coin Flip 提升最显著）；**上下文长度受限场景**（Narrative QA 书籍减 74.6%）；**设计新智能体通信协议**；**长程 Agent 流水线** | ⚠️ **弱模型担任发起方**（GPT-3.5 作 initiator 时常低于基线，甚至幻觉答案）；**信息必要性类任务**；**需 NL 丰富表达的任务**（开放式创作、共情对话）；**格式选择本身也有成本** |
| **DistServe** | **有延迟 SLO 约束的在线 LLM 服务**：Chatbot（重 TPOT）、Code Completion（双 SLO 严格）、Summarization（长输入 + 重 TPOT，**提升最大**） | **大规模分布式部署**（放置算法处理网络带宽约束）；跨节点集群（High/Low 两种算法） | ⚠️ **离线吞吐优先** ❌（chunked-prefill 更好）；**单 GPU / 极少 GPU** ❌；**不实现抢占与容错**（有故障传播风险）；⚠️ 新架构（GQA/MQA）表现更好但未实测 |

---

## 三、实验设置（七维之三）

| 论文 | 研究问题（RQ） | 评测设计 | 基线 / 对照 | 关键超参数 |
|---|---|---|---|---|
| **AgentOps** | ⚠️ **无 RQ（非实验论文）**。研究协议为：**检索字符串 → 数据源 → 选择标准 → 数据抽取项** | **系统映射研究协议**：多渠道路径检索 → 去重 → 筛选 → 抽取 | **选择标准 I1–I4**：I1 可观测性功能 / **I2 agent 级追踪（非仅 LLM 级）← 最关键门槛** / I3 正式发布版本 / I4 公开在线文档<br>**抽取项 D1–D7**：Name / Source / GitHub URL / **Star（流行度代理）** / Scope / Key Features / **Traceable Artifacts** | 五种功能类别全覆盖检查；**7 大功能 × 17 工具的完整矩阵** |
| **AgentSight** | **RQ1** 真实工作流中的性能开销？<br>**RQ2** 能否弥合语义鸿沟（检测安全威胁与性能病理）？ | **性能评估**：3 个真实开发者工作流，**开/关各跑 3 次**<br>**3 个案例研究**：提示注入 / 推理循环 / 多智能体协调 | 3 个基准工作流（基于 eunomia-bpf 教程仓库）：理解代码仓库 / 代码编写 / 仓库编译 | 时间邻近窗口 **100–500ms**；Case 2 用 crewai + gpt-4o-mini；Case 3 用 6 个 claude-code subagent |
| **Watson** | **RQ1** 重建的推理轨迹是否与主 agent 真实推理一致？<br>**RQ2** 观测过程是否损害主 agent 性能？（**观测者效应检验，最关键**）<br>**RQ3** 重建的推理是否对开发者有用？ | **三类真实 Agentware**：通用推理 agent（MMLU，大规模定量） + AutoCodeRover + OpenHands（真实性验证） | **RO-SR**（不开启 Watson）vs **WA-SR**（开启 Watson），各 3 次独立运行 | **FIM vs RepCoT** 两种重建法对照；LLM-as-a-judge **n=10**；统计用 **χ² + 95% CI + Fisher 检验**;MMLU 子集按 95% 置信 / 5% 误差设计 |
| **Who&When** | 能否自动定位"哪个 agent、哪一步"导致失败？ | **两种评测场景**（算法生成 + 手工打造）；**三个评价指标**（Agent 级 / Step 级 / 混合） | **3 种 LLM 归因方法**：All-at-Once / Step-by-Step / Binary Search + Hybrid；对比 **6 个模型 + o1 / R1** | 主模型 GPT-4o；推理提示词消融；容差下的 Step 级评估 |
| **AgentMonitor** | **RQ1** MAS 性能可预测吗（5 种划分）？<br>**RQ2** 训练数据量的影响？<br>**RQ3** 能否"偷看"（少量实例）？<br>**RQ4** 各类指标关系？<br>**RQ5** 能否缓解恶意 Agent 风险？ | **5 种从易到难的划分** + **5 折交叉验证** + 误差棒：In-Task / In-Arch / In-Domain / Cross-Arch / Cross-Task<br>**安全实验**：两个额外架构（无害-有用 / 含恶意 Agent） | **Post-edit 位置对照**（Table 3）：恶意 Agent 后 / 中间位置 / 所有 Agent 后 / summarizer 后；**对照组**：单独使用 u8B/8B/70B | XGBoost `reg:squarederror` + 网格搜索；只使用"绝对误差 < 0.05"的实例隔离噪声 |
| **AgentDropout** | **RQ1** 能否同时提升性能并降低 token？<br>**RQ2** 换初始拓扑/dropout 率/训练集是否仍有效？<br>**RQ3** 提升来自"有学习地剪"还是"链变短"？ | **五类基线对照** + **结构鲁棒性实验**（分层图/随机图替代全连接图）+ **dropout 率扫描** + **消融（Random Dropout 对照）** + **领域迁移（4×4 完整交叉矩阵）** | **Vanilla / CoT / MAS_round=1 / MAS_round=T / AgentPrune（SOTA）**；⚠️ **公平性设计**：AgentPrune 与 AgentDropout **都基于 MAS_round=T 优化**，且**直接采用 AgentPrune 的配置文件** | temperature=**1**；M∈**{10,20}**；η=**0.1**；α∈**{0.1,0.2}**；β∈**{0.1,0.2}**；**训练样本仅 40 条**；T=**2**（推理/数学）、T=**4**（代码） |
| **Beyond NL** | 六个 RQ（覆盖推理提效、通信降本、格式消融、泛化、迁移、ACL 对照） | **单模型推理**：5 个数据集 × 3 模型，报告 **3 次运行均值±标准差**，正则抽取<br>**多智能体通信**：双 Agent 分离上下文，RougeL + token 数 | **格式消融（Table 6）**：六种固定格式对照<br>**两步 AutoForm 消融（Table 3）**：Instance-Based vs Task-Based；同质 vs 异质<br>**传统 ACL 对照（Table 4）**：KQML vs JSON-KQML | ⚠️ **未报告温度/top-p 等采样参数**；模型版本精确到 `gpt-4-1106-preview` |
| **DistServe** | 在 TTFT/TPOT 双 SLO 约束下，分离能否提升 per-GPU goodput？ | **三个真实应用 + 对应 SLO**（Table 1）；**主指标：SLO Attainment**（默认 90%，附录 99%）；两个自变量：**Per-GPU Rate** / **SLO Scale** | **vLLM**（共置，intra-op=1/4/8）、**DeepSpeed-MII**（chunked-prefill，⚠️ **无法服务 OPT-175B**）、**vLLM++**（消融，枚举最优并行策略） | **⭐ 消融设计**：vLLM vs vLLM++（证明"光调并行策略救不了共置"）；DistServe-High vs Low（量化跨节点带宽损失）；Simulator vs Real System |

---

## 四、实验平台（七维之四）

| 论文 | 硬件 / 系统 | 模型 | 框架 / 实现 | 透明度评价 |
|---|---|---|---|---|
| **AgentOps** | ⚠️ **无实验平台**。研究对象是**17 个 AgentOps 工具**（截至 2024-11） | — | — | 工具清单：Agenta(1.3k) / AgentNeo(1k) / AgentOps(2.1k) / **AGIFlow(21)** / Arize(3.9k) / DataDog(2.9k) / **Dify(51.6k)** / Helicone(1.9k) / Laminar(1.1k) / **Langfuse(6.5k)** / **LangSmith(417)** / LangTrace(552) / Lunary(1.1k) / PortKey(6.3k) / TraceLoop(3.4k) / Trulens(2.2k)。**14/17 有数千星标**；**仅 4 个明确为 Agents 范围** |
| **AgentSight** | **Ubuntu 22.04 / Linux 6.14.0**；单台服务器（⚠️ 未给 CPU/内存规格） | **Claude Code 1.0.62**，后端 **Claude 4**；Case 2 用 gpt-4o-mini | **eBPF**（uprobe + tracepoint + kprobe）；拦截 OpenSSL 的 `SSL_read`/`SSL_write`；**Rust/C 6000 行 + TypeScript 3000 行** | ⚠️ 未报告容器/K8s/Windows/macOS 验证；未给硬件规格 |
| **Watson** | Huawei Canada + Queen's University 环境 | 重建：GPT-3.5-turbo-instruct（FIM）/ GPT-3.5-turbo-0125（RepCoT）/ Qwen3-8B；汇总：GPT-4o-mini；被测：AutoCodeRover、OpenHands | 统计工具：χ² / 95% CI / Fisher 检验 | ⚠️ **成本完全不透明**（10 条轨迹 + 归因验证的开销未报告）；模型偏旧（GPT-3.5） |
| **Who&When** | ICML 2025 | 主模型 **GPT-4o**；对比 6 个模型 + **o1 / R1** | 构造框架：**AG2 CaptainAgent + Magnetic-One** | ⚠️ 无显著性检验与 IAA；样本仅 184 条、手工系统只有 1 个 |
| **AgentMonitor** | 港科大 + 清华；**开源** github.com/chanchimin/AgentMonitor | LLM 池：**Llama3-8B / Llama3-70B / ChatGPT**；下游：HumanEval / MMLU / GSM8K | **XGBoost**（Chen & Guestrin 2016），`reg:squarederror`，网格搜索；接入：`await monitor.register([...])` 一行注册（借鉴 PEFT / OpenDelta 的 wrapping 思路）；**验证方式**：5 折交叉验证 + 误差棒 | ✅ 开源；✅ 报告 5 种架构与完整数据规模 |
| **AgentDropout** | **NVIDIA A800 GPU**；推理框架 **vLLM** | Llama3-8B-Instruct / Qwen2.5-72B-Instruct（vLLM on A800）；**Deepseek-V3-671B-Instruct（API）** —— 跨度近 **84 倍（8B→671B）** | 智能体配置**直接沿用 AgentPrune**；DAGSample 采样 | ⚠️ **未给出 GPU 数量、训练总时长、单次实验耗时** → 规模化复现成本不明确；✅ 报告 prompt/completion tokens |
| **Beyond NL** | 清华 + 腾讯 + 北邮；**开源** github.com/thunlp/AutoForm | `gpt-3.5-turbo-1106`、`gpt-4-1106-preview`、`Gemini Pro 1.0` | **纯 prompt engineering**（无微调、无工具调用、无外部解释器）；答案用**正则表达式**抽取；RougeL 用 Google 官方实现；**3 次运行** | ✅ 模型版本精确到子版本；⚠️ **完全未报告采样参数**（格式选择可能对 temperature 敏感） |
| **DistServe** | **4 节点 / 32 × NVIDIA SXM A100-80GB**；节点内 NVLINK；**跨节点仅 25Gbps**；算法运行时间在 **AWS m5d.metal（96 核）** 上测 | **OPT-13B / OPT-66B / OPT-175B**，**FP16**；**故意选经典 MHA 的 OPT 以施加最大传输压力**（新 GPT 用 GQA/MQA 会更好） | 代码 **6.5K 行 Python + 8.1K 行 C++/CUDA**；技术栈 NCCL + 异步 CudaMemcpy + Ray actor + continuous batching + FlashAttention + PagedAttention；支持 OPT/LLaMA；OpenAI API 兼容前端 | ✅ 报告完整硬件与算法耗时；⚠️ 仅 OPT 系列（未实测 GQA/MQA）；跨节点仅 25Gbps → Dist-High 效果依赖仿真 |

---

## 五、数据集来源（七维之五）

| 论文 | 数据来源 | 规模 | 特点 / 评价 |
|---|---|---|---|
| **AgentOps** | ⚠️ **无 ML 数据集**。三类检索渠道：**GitHub**（37+618+81+24 初始仓库 → 11 工具） / **Google Search**（→6 工具，捕获专有工具） / **灰色文献**（博客、产品公告） | **最终 17 个工具**；抽取 D1–D7 元数据 | 方法学依据 **Garousi [7] 的"多声部文献综述"**；**星标数是唯一定量指标且明确定义为流行度代理**；**可追踪工件 D7 是分类法的原始素材**；⚠️ `"Agent" AND "DevOps"` 的 618 仓库**只筛前 3 页** |
| **AgentSight** | ⚠️ **无公开数据集**。基准用 eunomia-bpf 教程仓库；攻击/循环场景**均自建**；威胁模型参考 InjecAgent [30] 与 Breaking Agents [31] | 1 个攻击链 + 1 个循环场景 + 1 个 6-agent 场景 | ⚠️ **方法学上最明显的短板**——攻击链是**单条手工构造**，未在公开基准上批量评测，**无法给出检出率/误报率的量化指标** |
| **Watson** | **MMLU**（14,042 题 → 3 个随机子集 × **374 题**，按 95% 置信 / 5% 误差设计）；**SWE-bench-lite**（300 issue → AutoCodeRover **103** 有效 / OpenHands **57** 有效） | MMLU 374 题/轮 × 3 轮；代码 103 / 57 | ✅ MMLU 子集规模**有明确设计依据**（置信区间反推）；⚠️ 代码任务样本仅 103/57，**统计功效不足** |
| **Who&When** | **Who&When 基准**（自建）：query 来自 **GAIA + AssistantBench**；含**算法生成**（126 条，最大 10 步）与**手工打造**（Magnetic-One，58 条，最大 130 步） | **127 个多智能体系统 / 184 个失败标注任务** | ✅ **标注流程最扎实**：3 位专家三轮标注，成本约 **84 人时**；⚠️ 样本仅 184 条、**手工系统只有 1 个** |
| **AgentMonitor** | 数据点构造：`{各种指标, 下游任务表现}`；下游任务 **HumanEval / MMLU / GSM8K**（实例取自 **MINT**）；安全数据集 **BeaverTails / MaliciousInstruct / AdvBench** | **1,796 个数据点**；3⁴=81 组合/架构；5 种架构；LLM 池 3 个；3 任务 | ✅ 同时覆盖**性能预测**与**安全评测**两类数据；⚠️ 需域内历史数据（冷启动不可用） |
| **AgentDropout** | **六个公开标准基准**：**MMLU**（通用推理）、**GSM8K / MultiArith / AQuA / SVAMP**（数学）、**HumanEval**（代码）；全部标准学术数据集，**无自建、无人工标注** | 训练样本**仅 40 条**（从训练/验证集抽）；Table 6 做数学集 **4×4 完整交叉迁移** | ✅ 6 基准覆盖三类能力；✅ 40 条样本成本极低且**拓扑学习与模型训练解耦**；✅ **4×4 完整交叉矩阵**设计扎实；⚠️ **未报告测试集规模**（无法判断统计功效）；⚠️ 数学占 4/6 任务分布略偏；⚠️ 无真实工程任务（都是"考试题"） |
| **Beyond NL** | **推理（5 个）**：Logic Grid Puzzle / Information Essentiality / Minute Mysteries QA（Big-Bench）、Coin Flip（HuggingFace 前 500 条）、AQuA（测试集 254 条）<br>**通信（3 个）**：Hotpot QA / Wiki Hop / Narrative QA（各 100 条，改造为双 Agent 分离上下文） | 推理 200/68/203/500/254；通信各 100 | ✅ **AQuA 与 Info Essen 用选项编号输出**（让正则抽取可靠 work，工程手段值得学习）；⚠️ 样本量小 |
| **DistServe** | **Chatbot → ShareGPT**（输入 avg 755.5 / 输出 avg 200.3）<br>**Code Completion → HumanEval**（164 题，输入 avg 171.3 / 输出 avg 98.2）<br>**Summarization → LongBench**（输入 avg 1738.3 / 输出 avg 90.7） | 3 个应用数据集 | ⚠️ **LongBench 被截断到 2048**（受 OPT 绝对位置嵌入上限限制），**削弱长输入实验强度**；到达时间用**泊松分布**生成（数据集无时间戳） |

---

## 六、结果验证（七维之六）

| 论文 | 主结果 | 最强数字 | 消融 / 对照 | 验证方式评价 |
|---|---|---|---|---|
| **AgentOps** | ⚠️ **无量化实验**。验证 = **分类法覆盖度论证 + 内部一致性分析** | **Tracing 17/17（100%）、Monitoring 17/17（100%）**；**Guardrails 6/17（~35%）**；Evaluation 15/17、Prompt Mgmt 8/17、Feedback 7/17、Customisation 5/17 | ① 功能覆盖矩阵（Table 4）<br>② 流行度分布（14/17 有数千星标）<br>③ 分类法"可兑现性"论证（LangSmith 评估三层、Langfuse 反馈二分实为真实实现）<br>④ **§5 坦承两条有效性威胁** | ⚠️ **分类法尚未被案例验证**（作者列为未来工作）；⚠️ 原文表格计数不一致（正文称 17，Table 3 仅 16 行、Table 4 仅 15 行） |
| **AgentSight** | **RQ1**：平均开销 **2.9%**（3.4% / 4.9% / 0.4%）<br>**RQ2**：三个案例全部成功 | 开销 **2.9%**；攻击链 **521 原始事件 → 37 关联事件**（压缩比 ~14:1）；Case 3 捕获 **3153 个事件** | 3 任务 × 3 次（开/关对照） | ⚠️ **无量化检测指标、无基线对比、样本量小**；⚠️ 未报告方差/置信区间；✅ 三案例都给出了可解释的自然语言根因 |
| **Watson** | **RQ1**：答案相同 307（76.2%）、不同 67（16.6%）；**答案相同时推理仍发散 30.9%**；答案不同时推理发散 **95.5%**（Fisher p < 10⁻²⁰）<br>**RQ2**：MMLU 与 SWE-bench-lite 上 RO-SR vs WA-SR **全部 p > 0.05、CI 重叠** → **零干扰**<br>**RQ3**：2 个 Django 案例证明"一句 hint 即修正" | **零干扰（p>0.05）**；Fisher p < 10⁻²⁰ | FIM vs RepCoT；3 次独立运行 | ✅✅ **统计规范明显优于同类**——唯一同时具备 **χ² + 95% CI + Fisher + 重复运行 + 报告样本量**，且诚实把不显著定性为 trend；⚠️ "重建忠实性"靠 LLM 互证，**有循环论证风险** |
| **Who&When** | **Agent 级最佳 ~53.5%、Step 级最佳 ~14.2%**（手工系统仅 **8.77%**）；**部分方法低于随机**；**o1 / R1 也不实用**；7 条 Finding；混合方法最优但**贵 8.7×** | Agent **53.5%** / Step **14.2%** | 3 种方法 + Hybrid；6 模型 + o1/R1；容差下 Step 级评估；推理提示词消融 | ✅✅ **诚实报告负面结果**（部分方法低于随机）；⚠️ 无显著性检验与 IAA；⚠️ 长日志（>93 步）准确率趋近 0%；⭐ **显式推理 prompt 提升 7.4%/4.4%，优于换更强模型** |
| **AgentMonitor** | **In-Domain Spearman 0.89**；难题平均约 **0.58**；误差中位数 ≈ 0<br>训练 **50%** 处饱和；**仅 10% 实例 Spearman 仍约 0.82**<br>安全：平均**有害性 −6.2%、有用性 +1.8%** | In-Domain **0.89** | ① 训练规模消融（50% 饱和）<br>② 实例数消融（10% → 0.82）<br>③ 指标关系分析（**大多数指标非单调 → 印证树模型的必要性**）<br>④ Post-edit 位置对照（**所有 Agent 之后插入最优**） | ✅ 5 折交叉验证 + 误差棒；✅ **"5 种划分"评估方法论值得迁移**（精确定位失效维度）；✅ "先诊断数据形状、再选模型"的做法扎实；⚠️ 跨架构仅 0.58 |
| **AgentDropout** | 平均分 **68.70 / 91.58 / 93.72**（Llama3 / Qwen2.5 / Deepseek-V3），比 AgentPrune 高 **+2.19 / +0.77 / +0.47**；token 降 **21.4%–24.4%（prompt）/ 16.1%–21.4%（completion）**，**同时降两类**；**丢弃率 0.2→0.8，token 降到 1/4、性能仅掉 2.69 分** | prompt token **−21.4%~24.4%**；性能 **+0.47~2.19** | ⭐ **Random Dropout 对照（Table 5）**：随机剪枝只能到 66.69，学习式可达 **68.70** → **排除了"提升只因推理链变短"**；**两阶段（68.70）> 单阶段（66.47）**；换分层/随机初始拓扑仍有效；跨数据集迁移全部优于 AgentPrune 同域结果 | ✅✅ **Random Dropout 对照是排他性论证的教科书做法**；⚠️ **无任何显著性检验**；⚠️ **Learned Edge Dropout 低于 Random** 这个反例未讨论；⚠️ 未报测试集规模、训练步数、拓扑学习开销 |
| **Beyond NL** | 推理：GPT-3.5 **+5.4%**（41.1→46.0）、Gemini Pro **+5.7%**、GPT-4 **+3.3%**<br>通信 token 最高 **−72.7%**（RougeL 0.63→0.70）<br>⭐ AutoForm vs KQML vs JSON-KQML：HotpotQA **115.0 vs 313.8 / 346.0 tokens**，RougeL 同为 0.76 | token **−72.7%**；Coin Flip **+16.3 分** | 格式消融（六种固定格式，GPT-3.5 在 Coin Flip 上无序列表 39.0 vs 有序列表 **86.0**）；两步 AutoForm 消融；传统 ACL 对照 | ⚠️ **无显著性检验**；⚠️ **提升幅度普遍小于标准差**（多处单元格）；⚠️ 未报告采样参数；✅ **诚实报告反例**（GPT-3.5 作 initiator 时下降，专设 Appendix C） |
| **DistServe** | **摘要级**：**7.4× 更多请求** 或 **12.6× 更严 SLO**<br>**Chatbot** vs vLLM **2.0×–4.6×** 速率 / **1.8×–3.2×** SLO；vs MII **1.6×–7.4×** / **1.7×–1.8×**<br>**Code** vs vLLM **5.7×** / **1.4×**<br>**Summary** vs vLLM **4.3×** / **12.6×** ⭐ | **7.4× 速率 / 12.6× SLO**；KV cache 传输 **< 0.1% 总延迟**（95% 请求 < 30ms） | ① vLLM vs vLLM++ → **性能完全相同**（证明"光调并行度救不了共置"）<br>② DistServe-High vs Low<br>③ 模拟器误差 **< 2%**<br>⭐ 最优放置案例（OPT-175B）：prefill inter-op=3/intra-op=3；decode inter-op=3/intra-op=**4**（**不对称，人工难寻**） | ✅ 延迟分解做得很干净（Fig. 10）；✅ §7 Discussion 自述不适用场景很诚实；⚠️ **SLO 凭经验设定**、**泊松到达假设**、**无统计检验**、**未报样本量** |

---

## 七、启示与局限性（七维之七）

| 论文 | 核心启示 | 局限性 | 自陈的后续方向 |
|---|---|---|---|
| **AgentOps** | ① **AgentOps 需要术语标准化**，本文提供了词汇表<br>② **Tracing 是共识（100%），Guardrails 是短板（35%）**——追踪层已红海，护栏层是蓝海<br>③ **评估必须超越"最终输出"**，走向步骤 + 轨迹<br>④ 显式 vs 隐式反馈是根本权衡（**"接受/拒绝最终输出"属隐式，可被动收集高质量信号**）<br>⑤ **把责任做成元数据**是对抗 Shared Accountability 的工程手段<br>⑥ 安全审计是可观测性的天然输出<br>⑦ **"跨度之间的边"是已知缺口** | ⚠️ **无实验验证**（分类法未案例验证，是归纳产物待演绎验证）<br>⚠️ **时效性短**（数据截至 2024-11）<br>⚠️ 只筛前 3 页存在系统性遗漏风险<br>⚠️ **星标数≠能力**<br>⚠️ **自陈遗漏"步骤间的链接与交互"**<br>⚠️ 表格计数不一致；缺乏"工具→用例"映射；无成本/性能维度 | ① **通过真实世界案例研究验证分类法**<br>② **开发 AgentOps 工具原型** |
| **AgentSight** | ① **观测点的选择比观测手段更重要**（找演化最慢的接口）<br>② **信任模型范式转变**：从"假设合作"→"在防篡改边界强制观测"<br>③ **"AI to watch AI"**——用 LLM 做语义层而非规则层<br>④ **双流关联是弥合鸿沟的关键**（三信号设计可复用）<br>⑤ **系统级视角能看见应用层永远看不见的东西**（文件锁争用）<br>⑥ 提供了清晰的领域地图（意图侧 vs 动作侧） | ⚠️ **论文本身没有 Limitation 章节**（6 页短篇）<br>⚠️ **平台强绑定**（Linux + eBPF + OpenSSL）<br>⚠️ **只拦截 OpenSSL**，Go/Rustls/BoringSSL 等不覆盖<br>⚠️ **无量化检测指标、无基线、无阻断能力**<br>⚠️ 观察者 LLM 自身未评估 | （批判性归纳）跨平台验证、扩展到更多加密库、增加量化检测指标 |
| **Watson** | ① **问题定位漂亮**——先证伪朴素方案再设计替代<br>② **旁路重建 + 镜像原则**优雅（观测不干扰）<br>③ **三层可观测性模型**是清晰的领域地图<br>④ **统计规范是同类工作标杆** | ⚠️ **"重建忠实性"缺乏人类金标准**（LLM 互证有循环论证风险）<br>⚠️ **成本完全不透明**<br>⚠️ 代码任务样本仅 103/57，**统计功效不足**<br>⚠️ 模型偏旧（GPT-3.5）<br>⚠️ **未与 CoT / 机制可解释性方法做 baseline 对比**<br>⚠️ 只覆盖文本推理 agent | 建立人类金标准、报告成本、扩展到更多 agent 类型 |
| **Who&When** | ① **问题定义漂亮** + 基准与标注流程扎实<br>② **诚实报告负面结果**<br>③ Finding 1/2 揭示的「**感受野 vs 聚焦**」根本权衡<br>④ **⭐ 显式推理 prompt 提升 7.4%/4.4%，优于换强模型**（最可操作） | ⚠️ **绝对性能远不可用**（Step 级 14.2%）<br>⚠️ **样本仅 184 条、手工系统只有 1 个**<br>⚠️ **无显著性检验与 IAA**<br>⚠️ 长日志（>93 步）准确率趋近 0%<br>⚠️ **未验证"归因后系统是否真能改好"** | 扩大基准规模、提升长日志下的定位能力、验证归因的闭环效果 |
| **AgentMonitor** | ① **"可预测性"是与"可观测性"互补的独立技术线**（不解释机制，只预测结果）<br>② **"配置决定型指标"（零成本）与"推理型指标"（高成本）分层**是可复用设计<br>③ **泛化边界要按维度拆开评估**（5 种划分方法论值得迁移）<br>④ **监控能力可顺手转化为安全能力**<br>⑤ **用非线性模型是"发现驱动"的**（先发现非单调，才选 XGBoost） | ⚠️ **跨架构泛化不足**（Cross-Arch/Task 平均约 0.58）<br>⚠️ **需要域内训练数据**（冷启动不可用）<br>⚠️ LLM 判官存在**低估偏差**<br>⚠️ 安全-有用性权衡难两全 | 提升跨架构泛化、降低冷启动门槛、改进 LLM 判官偏差 |
| **AgentDropout** | ① **「冗余的两种来源」是漂亮的分解**——节点冗余 + 边冗余（"看别人漏了什么"式创新）<br>② **「逐轮动态 vs 全局统一」一个改动两个收益**（性能 + completion token）<br>③ **Random Dropout 对照是排他性论证的教科书做法**<br>④ **"40 条样本学拓扑"** ——一次性拓扑学习 + 长期推理省钱的 ROI 极划算<br>⑤ **「丢弃率→性能降得慢、成本降得快」**从反面证明 MAS 通信冗余规模之大 | ⚠️ **提升幅度小且随模型变强急剧衰减（2.19→0.47）**<br>⚠️ **无任何显著性检验**<br>⚠️ **未报告测试集规模、训练步数、拓扑学习开销**（净利润无法核算）<br>⚠️ **Learned Edge Dropout 低于 Random** 这个反例未讨论<br>⚠️ **DAG 约束不支持循环辩论**<br>⚠️ α/β 全局统一与"逐轮动态"的核心论点存在张力<br>⚠️ "domain transferability"实为同域跨数据集 | 扩展到循环拓扑、逐轮自适应 α/β、报告完整开销 |
| **Beyond NL** | ① **"重新审视默认假设"的范式文章**（质疑共识 + 控制变量，低资源高产出）<br>② **"格式"是被严重低估的优化维度**——一行 prompt 省 72.7%<br>③ **"从 5 个输入推任务级通用格式"是高杠杆设计**（一次性决策、全任务复用）<br>④ **"自发涌现的 ACL"是值得深挖的理论线索**——结构化通信可能是自然吸引子<br>⑤ **诚实报告反向结果是分析质量的分水岭** | ⚠️ **无显著性检验**<br>⚠️ **未报告采样参数**<br>⚠️ **样本量小**<br>⚠️ **格式分布靠人工统计**<br>⚠️ **提升幅度普遍小于标准差**<br>⚠️ GPT-3.5 作 initiator 时的下降需专门优化提示词 | 为弱模型优化提示词、扩大样本、增加显著性检验 |
| **DistServe** | ① **"识别系统级反模式"的经典范式**——从"大家都在做"中找错<br>② **目标函数选错，整个设计方向就错了**（比"分离"这个手段更有普适价值）<br>③ **"分离"是可迁移的系统设计模式**——判据：当两个负载的计算特征与优化目标根本不同时，共置必然导致效率损失<br>④ ⚠️ **关于"平行工作"的重要警示**：Splitwise / Tetris-Infer / DéjàVu 三篇同期并行工作 → **该方向已接近饱和** | ⚠️ **SLO 凭经验设定**（无公开 SLO，结论对取值高度敏感）<br>⚠️ **泊松到达假设**（真实负载有突发性，pull-based 机制正是为突发设计但**未实验验证**）<br>⚠️ **未报告采样条数、无统计显著性检验**<br>⚠️ **LongBench 被截断到 2048**（长上下文论证是**理论外推而非实验**）<br>⚠️ **仅 OPT 系列**（未测 GQA/MQA）<br>⚠️ **不含抢占与容错**（分离引入故障传播风险） | **抢占**（FCFS 有"护航效应"）；**容错**（分离引入了新的故障传播风险） |

---

## 八、横向对比速查表

### 8.1 评估严谨度对比（暴露方法论差异）

| 论文 | 显著性检验 | 样本量报告 | 误差/方差展示 | 报告负面结果 |
|---|:---:|:---:|:---:|:---:|
| **Watson** | ✅ χ² + 95% CI + Fisher | ✅（103/57/374） | ✅ 重复 3 次 | ✅ 诚实定性为 trend |
| **Who&When** | ❌ | ✅（184 条） | 部分 | ✅✅ **部分方法低于随机** |
| **DistServe** | ❌ | ❌ | 部分（延迟分解） | ✅ §7 自述不适用场景 |
| **AgentMonitor** | ❌ | ✅（1,796 点） | ✅ 5 折 CV | 部分 |
| **AgentDropout** | ❌ | ❌（未报测试集规模） | ✅ 多拓扑验证 | ⚠️ 反例未讨论 |
| **Beyond NL** | ❌ | ❌ | ✅（±标准差，3 次） | ✅ 报告 GPT-3.5 反例 |
| **AgentSight** | ❌ | ❌（样本小） | 部分（3 场景开销） | ✅ 自陈无基线 |
| **AgentOps** | — | — | — | ✅✅ §5 专门列 Threats to Validity |

> **观察**：**Watson 是统计规范最完整的**（唯一同时具备显著性检验 + 样本量报告 + 重复运行）；**DistServe 在系统论证完整性上最强**（提出新架构后系统性回答"代价是什么、边界在哪"）。**理想论文应同时具备两者。**

### 8.2 降本手段对比

| 论文 | 降本手段 | 侵入性 | 收益 |
|---|---|---|---|
| **Beyond NL** | **只改一句提示词** | **零侵入（纯 prompt）** | token 最多 **−72.7%** ⭐ 性价比最高 |
| **AgentDropout** | 剪掉冗余 agent/边 | 改拓扑（需训练，但仅 40 条样本） | prompt token **−21.4%~24.4%** |
| **DistServe** | 分离 prefill/decode | 重构服务架构 | **7.4× 速率** / **12.6× 更严 SLO** |

> **三层降本类比**：**Beyond NL = 把话说短**（表达层）；**AgentDropout = 少叫几个人**（拓扑层）；**DistServe = 把会议室重新分配**（物理资源层）——**分别作用于表达、拓扑、物理资源三个正交维度，理论上可完全叠加。**

### 8.3 三组正交关系

| 正交维度 | 第一篇 | 第二篇 | 叠加效应 |
|---|---|---|---|
| **表达 × 物理资源** | Beyond NL（token −72.7%） | DistServe（7.4× 速率） | token 压缩同时改善 TTFT 与 TPOT → **两层降本可叠加，尚无人做** |
| **拓扑 × 表达** | AgentDropout（少叫几个人） | Beyond NL（把话说短） | 分别作用于"通信图"与"消息内容"，**正交可叠加** |
| **规范 × 采集** | AgentOps（该有什么字段） | AgentSight（怎么零侵入填满） | **完整可观测性方案 = 分类法 × eBPF 采集** |

### 8.4 收益随模型变强的反差

| | AgentDropout | DistServe |
|---|---|---|
| 收益随模型变强 | **衰减**（2.19 → 0.77 → 0.47） | **更显著**（KV cache 传输压力大、共置干扰更严重） |
| 原因 | 弱模型算法层冗余更多 | 大模型系统层冗余更多 |

> **"算法层冗余"与"系统层冗余"遵循完全不同的规律。**

---

## 九、研究缺口与选题建议

| # | 缺口 | 依据 | 可行性 |
|---|---|---|---|
| **1** | **把"跨度之间的边"形式化进 AgentOps 分类法** | AgentOps 分类法**自陈遗漏** trace links / interactions；而 **AgentDropout 优化边、AgentSight 捕捉跨边界交互、Who&When 度量步骤间影响**——三篇各自触及却无人统一形式化 | ⭐⭐⭐⭐⭐ |
| **2** | **用 AgentOps 的 Span 类型作 Who&When 归因的结构化输出空间** | AgentOps 提供"地址空间"，Who&When 提供"归因方法"，无人对接 | ⭐⭐⭐⭐ |
| **3** | **Guardrails 的可追踪化与自动化安全案例生成** | AgentOps 显示护栏覆盖率仅 **35%**（对比追踪 100%） | ⭐⭐⭐⭐ |
| **4** | **"格式压缩 × 服务分离"联合优化** | Beyond NL（−72.7%）与 DistServe（7.4×）正交可叠加，无人做过 | ⭐⭐⭐⭐ |
| **5** | **AgentOps 分类法的演绎验证** | 作者明确列为未来工作 | ⭐⭐⭐ |
| **6** | **跨架构泛化的性能预测** | AgentMonitor 跨架构 Spearman 仅 **0.58**（域内 0.89） | ⭐⭐⭐ |
| **7** | **归因后的效果闭环验证** | Who&When 明确**未验证"归因后系统是否真能改好"** | ⭐⭐⭐ |
| **8** | **Implicit Feedback 的自动化采集** | AgentOps 指出隐式反馈更难但更可靠 | ⭐⭐⭐ |

### 候选选题（综合 #1 + #2）

> **"基于 Span 图的 Agent 故障归因：把拓扑结构纳入可观测性分类法"**
>
> **做法**：① 在 AgentOps 的 9 类 Span 基础上补充**"跨 Span 边"的类型学**（数据依赖边 / 因果边 / 责任边）；② 用 AgentSight 的系统边界信号作为**边的廉价观测手段**；③ 把 Who&When 的反事实归因从"步骤级"提升到"**边级**"；④ 用 AgentDropout 的拓扑学习做干预验证闭环。
>
> **差异化**：现有归因只看**节点**（谁错），本选题看**边**（信息在哪里断），形成**"观测 → 归因 → 干预 → 复验"的完整闭环**——恰是 8 篇论文各占一段、无人打通的全链。

---

## 十、一句话总结

> **AgentOps 定义"该看什么"（元层），AgentSight 与 Watson 分别实现"看动作"与"看想法"（观测），Who&When 与 AgentMonitor 负责"找责任"与"猜结果"（诊断），AgentDropout、Beyond NL、DistServe 从拓扑、表达、资源三个正交维度做"改造"（干预）。**
>
> **—— 8 篇论文合起来是一条完整的"观测 → 诊断 → 干预"链路，但链路的关节（尤其是"跨度之间的边"与"归因后的闭环验证"）仍然断开。**

---

## 附：8 篇精读笔记索引

| # | 精读笔记文件 | 论文 | 最强指标 |
|---|---|---|---|
| 1 | `AgentSight_论文精读笔记.md` | AgentSight: boundary tracing via eBPF | 开销 2.9%；521→37 事件压缩 |
| 2 | `WhichAgentCausesTaskFailures_论文精读笔记.md` | Which Agent Causes Task Failures and When? | Agent 53.5% / Step 14.2% |
| 3 | `Watson_论文精读笔记.md` | Watson: cognitive observability | 零干扰（p>0.05）；推理发散 30.9% |
| 4 | `AgentDropout_论文精读笔记.md` | AgentDropout: dynamic agent elimination | token −21.4%~24.4%，性能 +0.47~2.19 |
| 5 | `AgentMonitor_论文精读笔记.md` | AgentMonitor: plug-and-play MAS monitoring | In-Domain Spearman 0.89 |
| 6 | `BeyondNaturalLanguage_论文精读笔记.md` | Beyond Natural Language (AutoForm) | token −72.7%；准确率 +3.3~5.7% |
| 7 | `DistServe_论文精读笔记.md` | DistServe: disaggregating prefill and decoding | 7.4× 速率 / 12.6× SLO |
| 8 | `AgentOps_论文精读笔记.md` | AgentOps: enabling observability of LLM agents | Tracing 100% vs Guardrails 35% |

*调研（表格版）整理完毕。所有数字均可在对应精读笔记中溯源。*
