# 调研补充：2026-09-18 新增论文与缺口验证

> **补充目的**：在 2026-09-15 完成 8 篇核心论文调研的基础上，① 精读剩余 2 篇 PDF；② 验证研究缺口 #1 的真实性与新颖性；③ 追踪 Who&When 后续工作与领域最新进展；④ 据此细化候选选题的差异化定位。
>
> **核心结论**：领域在过去 12 个月内快速发展，出现了 6 篇直接相关工作。原候选选题的核心创新点（"边级归因"）已被部分覆盖，但**"观测分类法 × 边类型学 × 系统级观测 × 闭环干预"的完整统一框架**仍是空白。

---

## 一、新增论文精读（2 篇 PDF）

### 9. Understanding the Information Propagation Effects of Communication Topologies in LLM-based MAS

| 维度 | 内容 |
|---|---|
| **论文** | Understanding the Information Propagation Effects of Communication Topologies in LLM-based Multi-Agent Systems |
| **发表** | **EMNLP 2025 (main)**；arXiv:2505.23352 |
| **单位** | 吉林大学 + Griffith University + UNSW |
| **核心问题** | 为什么稀疏/密集拓扑各有优劣？信息（正确或错误）如何在拓扑中传播？ |
| **核心方法** | ① **CAPE（Counterfactual Agent Propagation Effect）**：通过反事实干预量化单个 agent 输出对最终决策的因果影响；② **TCTE（Topology-Conditioned Transfer Effect）**：度量拓扑对 agent 级扰动的敏感度；③ **EIB-Learner**：双视图 GNN（稀疏视图抑制错误传播 + 密集视图促进洞察传播）+ 查询自适应融合 |
| **关键发现** | ① **稀疏拓扑抑制错误传播但同时也抑制有益洞察**；② **密集拓扑促进洞察传播但也加速错误扩散**；③ **适度稀疏最优**——在错误抑制与洞察传播间取得平衡；④ 现有方法（AgentDropout/AgentPrune）**未显式平衡错误与洞察**，表现不优于随机中等稀疏图 |
| **最强指标** | 6 个基准数据集上 SOTA；通信成本降低 |
| **与已读论文的关系** | ① 与 **AgentDropout** 正交：AgentDropout 剪节点/边降 token，本文分析**为什么**该剪；② 引入了**反事实因果分析**到通信拓扑——与 Who&When 的反事实干预异曲同工；③ 首次把"信息传播"分解为"错误传播"和"洞察传播"两个正交维度 |
| **对选题的启示** | ⭐ **CAPE 指标可直接用于"边级归因"**——它度量的是"某 agent 的输出变化经拓扑传播后对最终结果的影响"，本质就是**边的因果传播能力**。这为"边级归因"提供了可量化的因果度量基础。 |

### 10. eInfer: Unlocking Fine-Grained Tracing for Distributed LLM Inference with eBPF

| 维度 | 内容 |
|---|---|
| **论文** | eInfer: Unlocking Fine-Grained Tracing for Distributed LLM Inference with eBPF |
| **发表** | **eBPF'25** (ACM Workshop, 2025-09)；Coimbra, Portugal |
| **单位** | University of Connecticut + UW + UC Santa Cruz + SJTU |
| **核心问题** | 分布式 LLM 推理管线的性能瓶颈如何零侵入定位？ |
| **核心方法** | ① **eBPF 拦截** GPU 驱动调用（ioctl）、内存操作、CPU 调度；② **Cooperative Kernel Proxies (CKP)**：在用户空间（PyTorch CUDA runtime / NCCL）嵌入轻量探针，与 eBPF 程序通过共享内存环形缓冲区通信；③ **跨层事件驱动执行图**：节点 = ML 操作（如 LlamaDecoderLayer.forward()、KV-cache copy、CUDA attention kernel），边 = PCIe 传输、CPU-GPU 上下文切换、NCCL 通信；④ **运行时自适应追踪**机制 |
| **关键贡献** | 首个 eBPF-based 端到端分布式 LLM 推理追踪系统；vendor-agnostic（不锁定 NVIDIA）；无需应用代码修改 |
| **与已读论文的关系** | ① 与 **AgentSight** 同属 eBPF 观测家族，但 eInfer 在**推理系统层**（GPU/分布式），AgentSight 在**智能体边界层**（应用/网络）；② 与 **DistServe** 互补：DistServe 做架构设计，eInfer 做运行时观测；③ eInfer 的"执行图"概念与 AgentTrace 的"因果图"类似，但粒度在内核/硬件层 |
| **对选题的启示** | ⭐ **eInfer 的"跨层执行图"提供了"边"的另一层观测手段**——在推理系统层，边的类型包括 PCIe 传输边、NCCL 通信边、CPU-GPU 上下文切换边。如果能将 AgentSight 的应用边界边与 eInfer 的系统内部边统一起来，可形成**跨层次的完整边观测体系**。 |

---

## 二、研究缺口 #1 验证：是否已有人给 AgentOps 分类法补过"边"？

### 2.1 检索策略

1. 搜索 "AgentOps taxonomy span types LLM agent observability follow-up" → 仅返回原论文，无后续工作扩展分类法
2. 搜索 "agent observability taxonomy trace link interaction between spans formalization" → 返回工业界博客和教程，无学术论文正式定义 span 间边类型
3. 检查 AgentOps 原文（arXiv:2411.05285v2, 2026-08 更新）→ 仍是 v2，无修订版补充边类型

### 2.2 验证结论

> **AgentOps 分类法的"边"缺口依然存在。** 截至 2026-09-18，未发现任何学术论文正式将"跨 Span 边"的类型学纳入 AgentOps 分类法。AgentOps 原文自陈的遗漏（"步骤间的链接与交互"）仍未被填补。

但需注意：**工业界实践已隐式使用了边概念**——OpenTelemetry GenAI 语义约定定义了 parent/child span 关系，Microsoft Agent Governance Toolkit 提到"causal trace IDs encode the full spawn tree — parent, child, and sibling relationships"。这些是工程实现，不是学术形式化。

---

## 三、领域最新进展追踪（2025-09 ~ 2026-09 新增 6 篇重要工作）

### 3.1 新增论文速览表

| # | 论文 | 发表 | 核心贡献 | 与原选题的关系 |
|---|---|---|---|---|
| 11 | **CDC-MAS** (Automatic Failure Attribution...) | arXiv:2509.08682, 2025-09 | 多粒度因果推断 + Shapley 值 agent 级归因 + CDC-MAS 关键步识别 + **闭环优化验证** | ⭐⭐⭐ **直接竞品**：Step 级 14.2%→36.2%，且已做闭环验证 |
| 12 | **EDGE** (Error Dependency Graph...) | arXiv:2609.01360, 2026-09 | **错误依赖图**（节点=错误类别，边=传播关系）+ Suppes 筛选 + CAPRI 剪枝 + 反事实验证 + 两阶段 LLM 判官 | ⭐⭐⭐⭐ **最直接竞品**：已做"边级"归因（但错误类别级，非 span 级） |
| 13 | **AgentTrace** | arXiv:2603.14688, 2026-03 | **因果图**（3 种边类型：sequential/communication/data-dependency）+ 反向 BFS + 17 特征排名 | ⭐⭐⭐ **高度相关**：已建因果图并定义边类型，但归因仍为节点级 |
| 14 | **TraceElephant** (Seeing the Whole Elephant) | ACL 2026 | **完整可观测性基准**（input+output+tool_logs+config+replayable_env），220 条失败 trace | ⭐⭐⭐ **基础设施**：证明完整可观测性对归因至关重要 |
| 15 | **MAST** (Why Do Multi-Agent LLM Systems Fail?) | NeurIPS 2025 D&B | **1600+ 标注 trace，7 种 MAS 架构，失败模式分类法** | ⭐⭐ **重要基准**：EDGE 和 CDC-MAS 均在此评测 |
| 16 | **Understanding the Info Propagation** | EMNLP 2025 main | **CAPE 因果度量 + EIB-Learner**，分析错误/洞察在拓扑中的传播 | ⭐⭐⭐ **理论支撑**：为"边的因果传播能力"提供量化度量 |

### 3.2 关键论文详细分析

#### 11. CDC-MAS: Automatic Failure Attribution and Critical Step Prediction (arXiv:2509.08682)

| 维度 | 内容 |
|---|---|
| **问题** | Who&When 的 Step 级准确率仅 14.2%，远不可用；统计相关性方法根本不足 |
| **方法** | ① **Performance Causal Inversion（性能因果反转）**：反转执行日志中的数据流，正确建模性能依赖；② **Shapley 值**：精确分配 agent 级归因；③ **CDC-MAS 算法**：处理 MAS 交互数据的非平稳性，识别关键失败步骤；④ **自动优化闭环**：归因结果→生成改进建议→反事实仿真验证 |
| **结果** | Step 级 **36.2%**（vs Who&When 14.2%）；任务成功率平均 **+22.4%** |
| **评测** | Who&When + TRAIL 基准 |
| **与原选题的冲突** | ⚠️ **CDC-MAS 已做了"归因→优化→验证"闭环**——这覆盖了原选题的第四步"闭环验证"。但 CDC-MAS 的闭环是"归因→提示词优化"（改 prompt），不是"归因→拓扑优化"（改结构）。**拓扑层面的闭环仍无人做。** |

#### 12. EDGE: Error Dependency Graph-Guided Multi-Error Attribution (arXiv:2609.01360)

| 维度 | 内容 |
|---|---|
| **问题** | 已有归因只找单个根因，但 MAS 失败通常包含**多个相关错误**，错误间有依赖传播 |
| **方法** | ① **错误依赖图** G=(C,R)：节点=错误类别，边=传播关系 A→B（A 使 B 更可能发生）；② **Suppes 筛选**（时间优先 + 概率提升）→ 观察相关图 G_S；③ **CAPRI 剪枝**（AIC 评分 DAG 搜索）→ 因果候选图 G_C；④ **反事实 rollout 验证**（修复源错误，观察下游是否消失）→ 验证图 G_V；⑤ **两阶段 LLM 判官**（Stage1 全图引导初始预测 → Stage2 trace 特定边子集精炼） |
| **结果** | 在 TRAIL 和 MAST 上提升多错误归因；图引导在多种 prompt 策略下均有效 |
| **与原选题的冲突** | ⚠️⚠️ **EDGE 是最直接的竞品**——它已做"边级"归因（错误类别间的传播边），且已用反事实验证边的因果性。但有三个关键区别：① EDGE 的边在**错误类别级**（taxonomy-level），不在**个体 span/agent 级**（per-trace）；② EDGE 不连接 AgentOps 可观测性分类法；③ EDGE 不做拓扑干预/优化闭环。 |

#### 13. AgentTrace: Causal Graph Tracing (arXiv:2603.14688)

| 维度 | 内容 |
|---|---|
| **问题** | MAS 失败诊断困难：级联效应、隐藏依赖、长执行轨迹 |
| **方法** | ① **因果图构建**：DAG，节点=agent 动作，**3 种边**（sequential / communication / data-dependency）；② **反向 BFS 追踪**：从错误节点出发收集祖先候选；③ **17 特征加权排名**（Position 权重 0.70 最高） |
| **结果** | Hit@1 **94.9%**，0.12s/trace（69× 快于 LLM 分析） |
| **局限** | ⚠️ **仅合成基准**（550 场景，10 域）；**仅单根因**；归因仍为**节点级**（找根因节点），不做边级归因 |
| **与原选题的关系** | ⭐ **AgentTrace 的 3 种边类型（sequential/communication/data-dependency）为"边类型学"提供了实践参考**，但它用边来排名节点，不直接归因到边。 |

#### 14. TraceElephant: Seeing the Whole Elephant (ACL 2026)

| 维度 | 内容 |
|---|---|
| **问题** | 已有归因基准只提供 output-only trace，不符合开发者调试场景 |
| **方法** | ① **完整 trace schema**：input_context + output_content + tool_logs + agent_configuration + system_architecture；② **多轮专家标注**（Krippendorff's alpha: agent 0.72, step 0.64）；③ **静态+动态评测**（动态配置提供可 replay 环境，支持反事实验证） |
| **结果** | Full trace 将 step 级准确率从 16%（output-only）提升到 28-30%；Dynamic Agentic 达 33.3% |
| **与原选题的关系** | ⭐ **TraceElephant 证明"可观测性质量决定归因质量"**——完整 trace 使 step 级准确率翻倍。这直接支持原选题"用系统级观测（AgentSight/eInfer）增强归因"的核心论点。 |

#### 15. MAST: Why Do Multi-Agent LLM Systems Fail? (NeurIPS 2025 D&B)

| 维度 | 内容 |
|---|---|
| **核心** | 1600+ 标注 trace，7 种 MAS 架构，失败模式分类法 |
| **意义** | **最重要的 MAS 失败分析数据集**——EDGE 和 CDC-MAS 均在此评测。提供了大规模真实失败案例。 |
| **对选题的启示** | 如果做"边级归因"实验，MAST + TraceElephant 是两个现成的评测基础。 |

---

## 四、原选题的差异化重定位

### 4.1 原选题回顾

> **"基于 Span 图的 Agent 故障归因：把拓扑结构纳入可观测性分类法"**
>
> 四步：① 补充"跨 Span 边"类型学 → ② 系统级信号作边观测 → ③ step 级归因提升到边级 → ④ 拓扑学习做干预验证闭环

### 4.2 竞品覆盖矩阵

| 原选题四步 | EDGE | CDC-MAS | AgentTrace | TraceElephant | Understanding Info | 是否仍空白 |
|---|:---:|:---:|:---:|:---:|:---:|:---:|
| ① 边类型学纳入可观测性分类法 | ❌ | ❌ | ⚠️ 部分（3种边，但不连分类法） | ❌ | ❌ | ✅ **仍空白** |
| ② 系统级信号作边观测 | ❌ | ❌ | ❌（日志重建） | ⚠️（完整日志但非系统级） | ❌ | ✅ **仍空白** |
| ③ step→边级归因 | ⚠️ 部分（错误类别级边） | ❌（仍为 step 级） | ❌（节点级） | ❌（基准非方法） | ⚠️（CAPE 度量边影响力） | ⚠️ **部分空白** |
| ④ 拓扑学习做干预验证闭环 | ❌ | ⚠️（提示词级闭环） | ❌ | ⚠️（replay 验证） | ⚠️（拓扑优化但非归因驱动） | ✅ **仍空白** |

### 4.3 修正后的选题定位

> **原选题**：基于 Span 图的 Agent 故障归因：把拓扑结构纳入可观测性分类法
>
> **修正选题**：**基于系统级因果图的 Agent 边级故障归因与拓扑干预闭环**
>
> **一句话**：现有工作要么在错误类别级做边（EDGE），要么在节点级做归因（AgentTrace/CDC-MAS），要么用日志重建因果图但不做归因（AgentTrace），要么做完整可观测性但不连归因（TraceElephant）。**本选题做的是：把系统级观测信号（eBPF/eInfer）作为"边"的廉价观测手段，将归因从节点级提升到 span 间因果边级，并将归因结果直接驱动拓扑优化（AgentDropout 式），形成"观测→归因→干预→复验"的完整闭环。**

### 4.4 修正选题的四步与差异化

| 步骤 | 内容 | 最近竞品 | 差异化 |
|---|---|---|---|
| **① 边类型学形式化** | 定义 3+ 种 span 间边：**数据依赖边**（一个 span 的输出被另一个 span 消费）、**因果传播边**（一个 span 的错误导致下游 span 出错，用 CAPE 度量）、**责任边**（一个 span 的决策使另一个 span 的失败不可避免）。将边类型纳入 AgentOps 9 类 Span 分类法，形成**Span+Edge 统一分类法** | AgentTrace 定义了 3 种边（sequential/communication/data-dependency）但不连分类法；EDGE 在错误类别级定义边 | **差异**：在**span 级**（非错误类别级）定义边，且**纳入 AgentOps 分类法**（连标准） |
| **② 系统级边观测** | 用 eBPF（AgentSight 的进程谱系+时间邻近+参数匹配三信号关联）作为**边的廉价观测手段**——不需要应用插桩就能重建 span 间的数据流和因果流。eInfer 的跨层执行图补充推理系统内部的边。 | AgentTrace 从**应用日志**重建因果图（需 agent 配合）；TraceElephant 依赖**开发者完整日志** | **差异**：**零侵入**（eBPF 边界追踪），不依赖 agent 自报；且**跨层次**（应用边界 + 系统内部） |
| **③ 边级归因** | 不是找"哪个 agent/step 错了"（节点级），而是找"**哪条信息传播路径断了/传错了**"（边级）。用反事实干预：修复某条边（如纠正 span A 传给 span B 的错误信息），看下游是否恢复。可借鉴 EDGE 的 Suppes 筛选+反事实验证 pipeline，但作用在 **span 级**而非错误类别级。 | EDGE 做错误类别级边归因；CDC-MAS 做 step 级因果归因；AgentTrace 做节点级排名 | **差异**：**span 间因果边级**归因（比 EDGE 细，比 AgentTrace 精准）；且**边级归因可直接解释"信息在哪里断"**而非"谁错了" |
| **④ 拓扑干预闭环** | 归因结果直接指导拓扑优化：如果某条边是高频失败边，用 AgentDropout 式拓扑学习**剪掉或降权该边**，然后**复验系统性能是否提升**。形成"观测→归因→干预→复验"闭环。 | CDC-MAS 做了提示词级闭环（改 prompt）；无人做拓扑级闭环 | **差异**：**拓扑结构级干预**（剪边/降权），而非提示词级；且闭环由归因驱动（**数据驱动拓扑优化**） |

### 4.5 完整技术路线图

```
                    ┌─────────────────────────────────────────────────────┐
                    │            AgentOps Span+Edge 统一分类法              │
                    │  (9 类 Span + 3 类 Edge: 数据依赖/因果传播/责任)       │
                    └───────────────┬─────────────────────────────────────┘
                                    │ 定义"该观测什么"
                                    ▼
              ┌─────────────────────────────────────────────┐
              │     系统级边观测 (eBPF / AgentSight)          │
              │  进程谱系 + 时间邻近 + 参数匹配 → 因果边重建    │
              │  + eInfer 跨层执行图 (推理系统内部边)          │
              └─────────────────┬─────────────────────────┘
                                │ 提供"边的廉价观测"
                                ▼
                    ┌───────────────────────────────┐
                    │    边级故障归因 (Span 间因果边)   │
                    │  Suppes 筛选 → CAPRI 剪枝       │
                    │  → 反事实 rollout 验证           │
                    │  → "哪条边断了/传错了"            │
                    └───────────┬───────────────────┘
                                │ 归因结果指导干预
                                ▼
                    ┌───────────────────────────────┐
                    │    拓扑干预与复验 (AgentDropout)  │
                    │  剪掉/降权高频失败边              │
                    │  → 复验性能是否提升               │
                    │  → "观测→归因→干预→复验" 闭环     │
                    └───────────────────────────────┘
```

---

## 五、对原调研.md 的修订建议

### 5.1 总览表扩充（8→16 篇）

| # | 论文 | 层次 | 一句话贡献 | 最强指标 |
|---|---|---|---|---|
| 9 | **Understanding Info Propagation** | 拓扑分析层 | CAPE 因果度量 + EIB-Learner 平衡错误/洞察传播 | 6 基准 SOTA |
| 10 | **eInfer** | 推理系统观测层 | eBPF 跨层执行图追踪分布式 LLM 推理 | 零侵入；亚毫秒开销 |
| 11 | **CDC-MAS** | 归因层（进阶） | 多粒度因果推断 + Shapley + 闭环优化 | Step 36.2%；成功率 +22.4% |
| 12 | **EDGE** | 边级归因层 | 错误依赖图 + Suppes/CAPRI + 反事实验证 | 多错误归因提升 |
| 13 | **AgentTrace** | 因果图追踪层 | 3 种边因果图 + 反向 BFS + 17 特征排名 | Hit@1 94.9%；0.12s |
| 14 | **TraceElephant** | 基准层 | 完整可观测性 + 可 replay 环境 | Full trace 使 step 16%→30% |
| 15 | **MAST** | 基准层 | 1600+ 标注 trace，7 种 MAS 架构 | NeurIPS 2025 D&B |
| 16 | (AgentOps 分类法) | 元层 | 9 类 Span 分类法（待补充边类型） | Tracing 100% / Guardrails 35% |

### 5.2 更新后的研究缺口排序

| # | 缺口 | 原可行性 | 更新后状态 | 依据 |
|---|---|---|---|---|
| **1** | 边类型学纳入 AgentOps 分类法 | ⭐⭐⭐⭐⭐ | ✅ **仍空白**（验证完成） | 无学术论文形式化 span 间边类型 |
| **2** | AgentOps Span 类型作 Who&When 归因结构化输出空间 | ⭐⭐⭐⭐ | ⚠️ **部分覆盖** | EDGE 用错误类别作归因空间，但非 AgentOps Span 类型 |
| **3** | Guardrails 可追踪化 | ⭐⭐⭐⭐ | ✅ **仍空白** | 无后续工作 |
| **4** | 格式压缩×服务分离联合优化 | ⭐⭐⭐⭐ | ✅ **仍空白** | 无后续工作 |
| **NEW** | **系统级边观测 × 边级归因 × 拓扑干预闭环** | — | ✅ **仍空白** | EDGE 做边但不连观测和拓扑；CDC-MAS 做闭环但是 prompt 级；AgentTrace 建图但不做边级归因 |
| **NEW** | **用 CAPE 度量做边级归因** | — | ⚠️ **理论可行但未实现** | Understanding Info Propagation 提供了度量工具，但未用于归因 |
| 5 | AgentOps 分类法演绎验证 | ⭐⭐⭐ | ✅ **仍空白** | 作者仍列为未来工作 |
| 6 | 跨架构泛化性能预测 | ⭐⭐⭐ | ✅ **仍空白** | AgentMonitor 无后续工作 |
| 7 | 归因后效果闭环验证 | ⭐⭐⭐ | ⚠️ **部分覆盖** | CDC-MAS 做了 prompt 级闭环验证，但拓扑级未做 |

---

## 六、下步行动计划

### 6.1 已完成（2026-09-18）

- [x] 精读 EDGE 全文（arXiv:2609.01360）——三阶段图构建（Suppes筛选→CAPRI修剪→反事实验证）+ 两阶段检测器；边在错误类别级，适配到 span 级可行
- [x] 精读 CDC-MAS 全文（arXiv:2509.08682）——PCI性能因果反转 + Shapley + CDC-MAS算法四阶段；闭环为 prompt 级，拓扑级闭环仍空白
- [x] 精读 AgentTrace 全文（arXiv:2603.14688）——3种边（Seq 61%/Comm 27%/Data 12%）+ 反向BFS + 17特征（Position w=0.70主导）；Hit@1=94.9% 但仅合成基准
- [x] 下载并分析 TraceElephant 数据集（arXiv:2604.22708）——220条失败trace，3个系统；全trace使step准确率+76%；role-aware归因原则可复用
- [x] 下载并分析 MAST 数据集（arXiv:2503.13657）——1642条trace，7个MAS框架；14失败模式3大类别；κ=0.88；可作为边级归因的节点标签

> **精读结论**：详见 `新增5篇论文精读笔记.md`。核心差异化确认：① 粒度更细（span间边级 vs EDGE的错误类别级/CDC-MAS的step级）② 观测更原生（eBPF零侵入 vs AgentTrace的日志重建/TraceElephant的API中间件）③ 干预更深（拓扑级边裁剪 vs CDC-MAS的prompt级）④ 因果更原理（CAPE因果度量 vs AgentTrace的17启发式特征）

### 6.2 实验设计初步思路

1. **边类型定义**：在 AgentTrace 的 3 种边基础上，增加第 4 种——**因果传播边**（用 CAPE 度量），第 5 种——**责任边**（用反事实干预定义：修复此边下游恢复）
2. **边观测**：用 AgentSight 的三信号关联机制重建 span 间数据流；用 eInfer 的执行图补充推理系统内部边
3. **边级归因**：借鉴 EDGE 的 Suppes→CAPRI→反事实 pipeline，但作用在 span 级边而非错误类别级边
4. **拓扑干预**：用 AgentDropout 的可训练加权图，但**边的权重初始化用归因结果**（高频失败边初始低权），而非随机初始化
5. **评测**：在 MAST + TraceElephant 上评测，新增**边级归因准确率**指标（哪条边被正确识别为失败传播路径）

### 6.3 专利申请要点

1. **核心创新**：将故障归因从"节点级"（谁错）提升到"span 间因果边级"（信息在哪里断），并形成归因驱动的拓扑优化闭环
2. **技术方案**：Span+Edge 统一分类法 → eBPF 零侵入边观测 → Suppes+反事实边级归因 → 归因驱动拓扑干预 → 复验
3. **与现有技术的区别**：EDGE 在错误类别级做边，本方案在 span 级做边；CDC-MAS 做 prompt 级闭环，本方案做拓扑级闭环；AgentTrace 从日志重建图，本方案从系统级信号零侵入重建图

---

*调研补充完成。所有新增论文均可在 arXiv 上溯源。原调研.md 的核心结论仍然成立，但选题需根据 EDGE/CDC-MAS/AgentTrace 的出现进行差异化重定位。*
