# 新增 5 篇论文精读笔记（2026-09-18 补充）

> 本文档汇总本次精读的 5 篇论文，按与选题相关性排序。每篇包含核心方法论、实验数据、与选题差异化定位。

---

## 1. CDC-MAS: 基于因果推断的 MAS 自动故障归因

> **论文标题**：Automatic Failure Attribution and Critical Step Prediction Method for Multi-Agent Systems Based on Causal Inference
> **作者**：Guoqing Ma, Jia Zhu, Hanghui Guo 等（浙江师范大学 / 港科大）
> **发布**：arXiv:2509.08682v1, 2025-09-10（AAAI 2026 录用）
> **代码**：未公开

### 1.1 核心方法论

**两阶段因果推断框架**：

**Stage 1 — SBSLocator（Agent 级归因）**：
- **性能因果反转（PCI）原理**：信息流方向 ≠ 性能因果方向。若上游 A 提供错误数据导致下游 B 失败，则 B 的性能表现由 A 的性能缺陷因果决定。因此，从执行日志构造的数据依赖图需**反转边方向**才能得到性能因果图。
  - 数据依赖图：`E_data = {(v_i, v_j) | output(v_i) ⊆ input(v_j)}`
  - 性能因果图：`E_causal = {(v_j, v_i) | (v_i, v_j) ∈ E_data}`
- **Shapley 值增强**：将每个 agent 视为合作博弈的参与者，用蒙特卡洛近似计算边际贡献，量化各 agent 的系统重要性。
- **瓶颈定位**：综合 Shapley 值和反事实改进潜力，计算 `BS_j = φ_j × (Y_cf - Y_orig) × I`

**Stage 2 — CPIdentifier（Step 级归因）**：
- **多维特征空间**：技术复杂度 F_tech ⊕ 交互复杂度 F_interact ⊕ 时序动态 F_temporal ⊕ 语义一致性 F_semantic
- **CDC-MAS 算法**（四阶段因果发现）：
  1. 上下文感知特征准备（Transformer 编码全局混淆向量 C_MAS）
  2. 时序因果结构发现（条件独立性检验，以 C_MAS 为条件）
  3. 混淆感知边定向（用 C_MAS 作为混淆代理区分因果与伪关联）
  4. 因果路径分析与关键步排名（计算平均因果效应 ACE）
- **反事实验证**：对候选步骤模拟"最优干预"，计算改进潜力 Δ_k

### 1.2 实验数据

| 数据集 | 指标 | CDC-MAS | 最佳基线 |
|--------|------|---------|----------|
| Who&When (Algo) | Step Acc | 36.2% | 25.5% (Step-by-Step) |
| Who&When (Hand) | Step Acc | 18.2% | 7.0% (Step-by-Step) |
| Who&When (Algo) | Agent Acc | 48.5% | 54.3% (All-at-Once) |
| Who&When (Hand) | Agent Acc | 56.8% | 55.2% (All-at-Once) |
| TRAIL (GAIA) | Step Acc | 44.6% | 24.8% (avg baseline) |
| TRAIL (SWE Bench) | Step Acc | 14.3% | 10.5% (avg baseline) |

**闭环优化实验**：
- 基线成功率：15.4% → 因果修复后：37.8%（+22.4pp）
- 随机修复仅达 18.3%，证明因果归因的精准指导有效

### 1.3 与选题差异化

| 维度 | CDC-MAS | 我们的选题 |
|------|---------|-----------|
| 归因粒度 | Agent 级 + Step 级 | **Span 间边级**（信息传播路径） |
| 因果图构造 | 从日志反转数据依赖 | **eBPF 零侵入观测边** |
| 闭环干预 | **Prompt 级**（重写提示词） | **拓扑级**（边裁剪/AgentDropout） |
| 混淆处理 | Transformer 编码 + 条件独立检验 | 系统级因果图自然消除混淆 |
| 评估基准 | Who&When + TRAIL | 可复用 Who&When + TRAIL + MAST |

**关键差异化**：CDC-MAS 的闭环是"诊断→重写 prompt→重跑"，我们的闭环是"诊断→裁剪拓扑边→重跑"，干预层次更深。CDC-MAS 从日志反推因果图，我们从 eBPF 实时观测边，无需事后重建。

---

## 2. EDGE: 错误依赖图引导的多错误归因

> **论文标题**：EDGE: Error Dependency Graph-Guided Multi-Error Attribution in Multi-Agent LLM Systems
> **作者**：Jun Hou, Priya Pitre, Yi Fang, Xuan Wang（Virginia Tech）
> **发布**：arXiv:2609.01360v1, 2026-09-01
> **代码**：https://github.com/JuneHou/EDGE

### 2.1 核心方法论

**三阶段错误依赖图构建**：

**Stage 1 — Suppes 筛选（观测相关性图 G_S）**：
- 时序优先性：`Prec(A,B) = |{T: t_A < t_B}| / |T_AB|`
- 概率提升：`ΔPR(A,B) = P(B|A) - P(B|¬A)`
- 通过两个阈值后保留为候选边，权重为两者的几何均值

**Stage 2 — CAPRI 修剪（因果候选图 G_C）**：
- 在 Suppes 筛选的 DAG 搜索空间中，用 AIC 风格的评分函数（对数似然 + 参数惩罚）选择最优 DAG
- 剪除间接/传递依赖，得到更稀疏的因果候选图

**Stage 3 — 反事实验证（验证图 G_V）**：
- 对 G_C 中每条边 A→B，用 LLM 修复模块对源错误 A 的 span 打补丁
- 重跑后续 trace（工具输出固定），检查下游错误 B 是否消失
- 用 Judge B 标注 B 是否仍存在，计算下游消除效应 `Δ(A→B)`
- 保留 `Δ > τ_val` 的边形成 G_V

**两阶段检测器**：
- Stage 1：完整图指导的初始错误预测 `Ê^(1)(T) = J(T, C, R_τ)`
- Stage 2：根据 Stage 1 预测结果激活下游假设，验证图建议的下游错误
- 推理图 `G_τ` = 分数过滤的观测边 ∪ 验证因果边

### 2.2 实验数据

| 模型 | 数据集 | Baseline F1 | EDGE F1 | Δ |
|------|--------|------------|---------|---|
| Mistral-Small-24B | TRAIL-GAIA | 24.06 | 34.15 | +10.09 |
| GPT-oss-120B | TRAIL-GAIA | 25.76 | 37.20 | +11.44 |
| Gemini-2.5-Pro | TRAIL-GAIA | 33.51 | 42.13 | +8.62 |
| Gemini-2.5-Pro | MAST | 28.46 | 29.50 | +1.04 |
| GPT-4o | MAST | 18.32 | 26.97 | +8.65 |

### 2.3 与选题差异化

| 维度 | EDGE | 我们的选题 |
|------|------|-----------|
| 图节点类型 | **错误类别**（taxonomy 级） | **Span**（执行步骤级） |
| 边语义 | 错误类别间传播概率 | Span 间信息传播路径 |
| 观测来源 | 跨 trace 统计 + LLM 修复验证 | **eBPF 实时观测** |
| 归因目标 | 多错误检测（multi-label） | 单故障根因路径定位 |
| 干预方式 | 无（仅归因+解释） | **拓扑级干预闭环** |
| 因果验证 | LLM 补丁 + 反事实重跑 | 系统级因果图 + CAPE 度量 |

**关键差异化**：EDGE 在错误类别级做边归因（如"工具选择错误"→"上下文失败"），我们在 span 级做边归因（如"Step 3 的输出→Step 5 的输入"）。粒度更细，能定位到具体信息传播路径而非抽象的错误类别传播。EDGE 的图是跨 trace 聚合的全局模式，我们的图是单次执行内的实时因果图。

---

## 3. AgentTrace: 因果图追踪的根因分析

> **论文标题**：AgentTrace: Causal Graph Tracing for Root Cause Analysis in Deployed Multi-Agent Systems
> **作者**：Zhaohui Geoffrey Wang（USC）
> **发布**：arXiv:2603.14688v2, 2026-03-27（ICLR 2026 Workshop on Agents in the Wild）
> **代码**：https://github.com/GeoffreyWang1117/AgentTrace

### 3.1 核心方法论

**因果图构建（3 种边类型）**：
1. **Sequential edges**（61%）：同一 agent 的连续动作，捕获推理流
2. **Communication edges**（27%）：消息发送→接收事件，跨 agent 通信
3. **Data dependency edges**（12%）：数据生产→数据消费，通过变量引用追踪

**反向 BFS 追踪**：
- 从错误节点 v_error 出发，沿父节点反向遍历，收集深度 d 内的所有祖先节点
- 算法复杂度 O(d × |E|)

**节点排名（5 组 17 特征）**：
| 特征组 | 权重 | 特征 |
|--------|------|------|
| Position | 0.70 | 归一化位置、到错误距离、深度比、反向位置 |
| Structure | 0.20 | 出度、入度、介数中心性、可达性 |
| Content | 0.05 | 错误关键词、不确定性、长度异常、关键词密度 |
| Flow | 0.03 | Agent 切换、角色关键性、通信类型 |
| Confidence | 0.02 | 声明确信度、模糊评分 |

- 评分公式：`score(v) = Σ w_i · F_i(v)`
- 权重通过 50 个场景的网格搜索确定

### 3.2 实验数据

| 方法 | Hit@1 | Hit@3 | MRR |
|------|-------|-------|-----|
| Random | 9.1% | 27.3% | 0.18 |
| LLM Analysis (GPT-4) | 68.5% | 81.4% | 0.74 |
| **AgentTrace** | **94.9%** | **98.4%** | **0.97** |

- 基准：550 个合成场景，10 个领域，8-15 步
- 运行时：0.12 秒/trace（比 LLM 快 69 倍）
- 鲁棒性：各类 bug 类型 93.6%-95.8%，各 trace 长度 93.9%-95.5%

### 3.3 与选题差异化

| 维度 | AgentTrace | 我们的选题 |
|------|-----------|-----------|
| 图构造来源 | **日志重建**（post-hoc） | **eBPF 实时观测**（zero-instrumentation） |
| 边类型 | 3 种（sequential/comm/data） | **Span 间因果边**（含 eBPF 观测的系统级边） |
| 排名方法 | 17 特征加权线性组合 | **因果度量（CAPE/ACE）** |
| 基准类型 | 合成场景（550 个） | 真实失败 trace（Who&When/TRAIL/MAST） |
| 干预闭环 | 无 | **归因驱动拓扑干预** |
| 单/多根因 | 仅单根因 | 支持多根因路径 |

**关键差异化**：AgentTrace 的核心局限是"假设执行日志准确可用"，但这在生产环境中不成立。我们用 eBPF 零侵入观测直接获取系统级边，不依赖日志质量。AgentTrace 的 17 特征排名是启发式的（Position 权重 0.70 主导），我们用 CAPE 因果度量做更原理性的排名。AgentTrace 无干预闭环，我们做归因→裁剪→重跑的完整闭环。

---

## 4. TraceElephant: 全可观测性基准

> **论文标题**：Seeing the Whole Elephant: A Benchmark for Failure Attribution in LLM-based Multi-Agent Systems
> **作者**：Mengzhuo Chen, Junjie Wang 等（中科院软件所）
> **发布**：arXiv:2604.22708v1, 2026-04-24（ACL 2026）
> **代码**：https://github.com/TraceElephant/TraceElephant

### 4.1 核心贡献

**全可观测性基准**：首个提供完整执行 trace（输入+输出+工具日志+配置）的故障归因基准。

**数据规模**：
- 380 条 trace（220 条失败），来自 3 个系统
- Captain-Agent (GAIA/AssistantBench)、Magentic-One (GAIA/AssistantBench)、SWE-Agent (SWE-Bench)

**Trace Schema**：
- **Trace 元数据**：task_id, task_instruction, system_name, agent_configuration, system_architecture
- **Step 输入**：step_id, agent_id, agent_name, input_context（完整输入信息）
- **Step 输出**：output_content, tool_logs（工具名、参数、输出、状态）

**归因原则**：Role-aware & Recoverability-aware
- 如果上游错误可被下游 verifier 纠正，但 verifier 未履行职责，则归因于 verifier 而非上游
- 失败归因于"恢复机会被错过的点"，而非"第一个可见错误"

### 4.2 实验数据

| 配置 | Agent Acc | Step Acc |
|------|-----------|----------|
| 部分可观测（仅输出） | ~53.7% | ~17.2% |
| 全可观测（静态） | 65.9% | 30.3% |
| 全可观测（动态+重放） | 68.2% | 33.3% |

- **全 trace 使 step 级准确率提升 76%**
- Step 级归因对缺失信息更敏感（+76% vs +22% agent 级）
- 动态配置（重放+反事实探测）额外提升 10%

### 4.3 与选题的关系

**直接支撑**：TraceElephant 证明了"可观测性越高，归因越准确"。这验证了我们用 eBPF 提供系统级可观测性的前提——更多观测维度（系统调用、网络 I/O、进程间通信）会进一步提升归因准确率。

**可复用**：
- Trace Schema 可作为我们系统的数据模型参考
- Role-aware 归因原则可纳入我们的边级归因逻辑
- 220 条失败 trace 可作为评估数据集
- 动态配置（重放+反事实）与我们的闭环干预理念一致

---

## 5. MAST: 多智能体系统失败分类法与数据集

> **论文标题**：Why Do Multi-Agent LLM Systems Fail?
> **作者**：Mert Cemri, Melissa Z. Pan 等（UC Berkeley）
> **发布**：arXiv:2503.13657v3, 2025-10-26（NeurIPS 2025 Datasets & Benchmarks Track）
> **代码/数据**：https://github.com/multi-agent-systems-failure-taxonomy/MAST

### 5.1 MAST 分类法

**14 个失败模式，3 大类别**：

| 类别 | 占比 | 失败模式 |
|------|------|---------|
| **系统设计问题** | 44.2% | 1.1 不遵守任务规范(15.2%)、1.2 不遵守角色规范(1.57%)、1.3 步骤重复(11.5%)、1.4 丢失对话历史(6.54%)、1.5 未意识到终止条件(2.09%) |
| **Agent 间不一致** | 32.3% | 2.1 对话重置(5.50%)、2.2 未请求澄清(2.09%)、2.3 任务偏离(6.02%)、2.4 信息隐瞒(5.50%)、2.5 忽略其他 Agent 输入(6.02%)、2.6 推理-行动不匹配(2.36%) |
| **任务验证** | 23.5% | 3.1 过早终止(8.64%)、3.2 无/不完整验证(4.71%)、3.3 错误验证(7.59%) |

### 5.2 MAST-Data 数据集

- **1642 条标注 trace**，来自 7 个 MAS 框架
- 框架：ChatDev, MetaGPT, HyperAgent, AppWorld, AG2(MathChat), Magentic-One, OpenManus
- 模型：GPT-4o, Claude-3.7-Sonnet, Qwen2.5-Coder, CodeLlama
- 任务：编程(ProgramDev), 数学(GSM-Plus/OlympiadBench), 通用(GAIA)
- 标注：人工标注(κ=0.88) + LLM-as-a-Judge 流水线(κ=0.77)
- 失败率：41%-86.7%（跨 7 个 MAS 框架）

### 5.3 与选题的关系

**直接复用**：
- MAST 的 14 个失败模式可作为我们边级归因图的**节点标签**
- 1642 条 trace 可作为**评估数据集**
- 3 大类别（系统设计/Agent 间不一致/任务验证）可作为**边类型分类**的上层结构
- LLM-as-a-Judge 标注流水线可用于自动化标注我们的边级标签

**案例启示**：MAST 的案例研究显示 ChatDev 的 CPO agent "不遵守角色规范"（模式 1.2），通过确保 CEO 有最终发言权的简单修复使成功率 +9.4%。这恰好是一个"边级"问题——CPO→CEO 的通信边断裂。我们的方法应该能自动检测这类边断裂。

---

## 综合差异化矩阵

| 维度 | EDGE | CDC-MAS | AgentTrace | TraceElephant | MAST | **我们的选题** |
|------|------|---------|------------|---------------|------|-------------|
| 归因粒度 | 错误类别级 | Agent+Step 级 | 节点级 | Step+Agent 级 | 失败模式级 | **Span 间边级** |
| 图节点 | 错误类别 | Agent-step 三元组 | Agent 动作 | Step | 失败模式 | **Span** |
| 图边 | 类别间传播 | 数据依赖反转 | Seq/Comm/Data | 无（基准） | 无（分类法） | **信息传播路径** |
| 观测方式 | 跨 trace 统计 | 日志重建 | 日志重建 | API 中间件 | 人工+LLM 标注 | **eBPF 零侵入** |
| 因果验证 | LLM 补丁+反事实 | 反事实模拟 | 无 | 重放+反事实 | 无 | **CAPE 因果度量** |
| 干预闭环 | 无 | Prompt 级 | 无 | 无 | 无 | **拓扑级（边裁剪）** |
| 基准数据 | TRAIL+MAST | Who&When+TRAIL | 合成 550 | 220 trace | 1642 trace | 复用上述全部 |

**核心差异化总结**：
1. **粒度更细**：在 span 间边级做归因，比 EDGE（错误类别级）、CDC-MAS（agent+step 级）、AgentTrace（节点级）都细
2. **观测更原生**：eBPF 零侵入观测边，不依赖日志质量（AgentTrace 的核心局限）、不需要 API 中间件（TraceElephant 的方式）
3. **干预更深**：拓扑级干预闭环（边裁剪/AgentDropout），比 CDC-MAS 的 prompt 级干预更彻底
4. **因果更原理**：用 CAPE 因果度量做边级排名，比 AgentTrace 的 17 启发式特征更原理性
